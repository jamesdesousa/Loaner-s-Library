<h1>All Items</h1>

<ul>
    <% @items.each do |item| %>
        <li>
          <img src= "<%= item.image %>">
          <%= link_to item.name, item_path(item) %>


         </li>

    <% end %>
</ul>
